#pragma once
#include "CapMessageGameDataChecker.h"

namespace CapMessageDirectorFunction {

    void tryShowCapMessageSystem(const al::IUseSceneObjHolder*, const CapMessageShowInfo*, const CapMessageGameDataChecker*);

} 